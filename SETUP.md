# React 重构设置指南

## 快速开始

### 1. 安装依赖

首先，将 `package-react.json` 重命名为 `package.json`，然后安装依赖：

```bash
# Windows PowerShell
Rename-Item package-react.json package.json
npm install

# 或者手动重命名文件，然后运行
npm install
```

### 2. 开发模式

启动开发服务器：

```bash
npm run dev
```

开发服务器将在 `http://localhost:5173` 启动。

### 3. 构建生产版本

```bash
npm run build
```

构建输出将在 `dist` 目录中。

### 4. 预览生产构建

```bash
npm run preview
```

### 5. 部署到 GitHub Pages

```bash
npm run deploy
```

## 项目结构

```
├── src/
│   ├── components/          # React 组件
│   │   ├── Header.jsx      # 导航头部（包含移动端抽屉菜单）
│   │   ├── Footer.jsx      # 页脚
│   │   └── Layout.jsx       # 布局包装器
│   ├── pages/              # 页面组件
│   │   ├── Home.jsx        # 首页（已完成）
│   │   ├── News.jsx        # 新闻页面（待完善）
│   │   ├── Research.jsx    # 研究页面（待完善）
│   │   ├── Publications.jsx # 出版物页面（待完善）
│   │   ├── Software.jsx    # 软件页面（待完善）
│   │   ├── Members.jsx     # 成员页面（待完善）
│   │   ├── Join.jsx        # 加入页面（待完善）
│   │   └── Contact.jsx     # 联系页面（待完善）
│   ├── data/               # 数据文件
│   │   └── navigation.js   # 导航配置
│   ├── App.jsx             # 主应用组件（路由配置）
│   ├── main.jsx            # 入口文件
│   └── index.css           # 全局样式
├── static/                 # 静态资源（图片、CSS、JS）
├── index.html              # HTML 模板
├── vite.config.js          # Vite 配置
└── package-react.json      # 依赖配置（需重命名为 package.json）
```

## 已完成的工作

✅ **项目基础结构**
- 使用 Vite 作为构建工具
- React 18 + React Router 6
- 基础配置完成

✅ **核心组件**
- Header 组件（响应式导航栏，支持移动端抽屉菜单）
- Footer 组件
- Layout 组件（布局包装器）

✅ **首页**
- Hero section（英雄区域）
- 实验室描述部分
- 干实验室和湿实验室图片展示
- 资助机构展示

✅ **路由配置**
- 所有主要页面的路由已配置
- 基础页面组件已创建

✅ **样式**
- 保留了所有原始 CSS 文件
- 响应式设计已实现
- Material Design 样式已集成

## 待完成的工作

🔄 **数据迁移**
- [ ] 将 `_members/*.md` 文件转换为 JSON 或 React 组件
- [ ] 将 `_posts/*.md` 博客文章转换为可加载格式
- [ ] 迁移其他数据文件（publications, research 等）

🔄 **页面内容**
- [ ] 完成 Members 页面（需要成员数据）
- [ ] 完成 News 页面（需要博客文章数据）
- [ ] 完成 Publications 页面
- [ ] 完成 Research 页面
- [ ] 完成 Software 页面
- [ ] 完成 Join 页面
- [ ] 完成 Contact 页面

🔄 **功能增强**
- [ ] 添加 Markdown 解析支持（用于博客文章）
- [ ] 添加搜索功能（可选）
- [ ] 优化图片加载
- [ ] 添加 SEO 支持

## 技术栈

- **React 18** - UI 框架
- **React Router 6** - 路由管理
- **Vite** - 构建工具
- **Material Icons** - 图标库
- **React Markdown** - Markdown 解析（已安装，待使用）

## 注意事项

1. **静态资源路径**：`static` 目录被配置为 Vite 的 `publicDir`，所以所有静态资源可以通过 `/static/...` 访问

2. **Base URL**：已配置为 GitHub Pages 路径 `/genomics-in-healthcare.github.io/`

3. **开发 vs 生产**：`siteConfig.baseurl` 会根据环境自动调整（开发环境为空，生产环境为 `/genomics-in-healthcare.github.io`）

4. **CSS 文件**：原始 CSS 文件（material-design.css, custom.css）通过 `index.html` 中的 `<link>` 标签加载

## 下一步

1. 重命名 `package-react.json` 为 `package.json`
2. 运行 `npm install` 安装依赖
3. 运行 `npm run dev` 启动开发服务器
4. 开始迁移各个页面的内容

## 问题排查

如果遇到问题：

1. **路径问题**：检查 `vite.config.js` 中的 `base` 配置
2. **静态资源加载失败**：确保 `static` 目录存在且包含所需文件
3. **路由问题**：检查 `BrowserRouter` 的 `basename` 属性
4. **样式问题**：确保 CSS 文件路径正确







