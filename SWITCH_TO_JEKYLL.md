# 切换回 Jekyll 版本指南

本指南说明如何将网站从 React 版本切换回 Jekyll 版本。

## 📋 前提条件

- 确保 Jekyll 相关文件完整（`_config.yml`, `Gemfile`, `_layouts/`, `_includes/`, `_posts/`, `_members/` 等）
- 确保已安装 Ruby 和 Bundler（用于本地测试）

## 🔄 切换步骤

### 方法一：通过 GitHub 设置切换（推荐）

#### 步骤 1：切换到 Jekyll 分支（如果存在）或创建新分支

```bash
# 如果已有 jekyll 分支
git checkout jekyll

# 或者创建新分支
git checkout -b jekyll
git push origin jekyll
```

#### 步骤 2：修改 GitHub Pages 设置

1. 进入 GitHub 仓库的 **Settings** 页面
2. 找到左侧菜单的 **Pages** 选项
3. 在 **Source** 部分，选择以下选项之一：
   - **选项 A（推荐）**：选择分支 `jekyll`，文件夹选择 `/ (root)`
   - **选项 B**：选择分支 `main`，文件夹选择 `/_site`（需要先构建）
   - **选项 C**：选择 "GitHub Actions" 并使用 Jekyll 工作流

#### 步骤 3：禁用 React 部署工作流（可选）

如果不想 React 版本自动部署，可以：

1. 进入 `.github/workflows/deploy.yml`
2. 临时禁用工作流（在文件开头添加注释或重命名文件）

或者修改触发条件，只在特定分支触发：

```yaml
on:
  push:
    branches:
      - react  # 只在 react 分支触发
```

#### 步骤 4：启用 Jekyll 部署

**如果使用 GitHub Actions（推荐）：**

1. 确保 `.github/workflows/deploy-jekyll.yml` 存在
2. 在 GitHub Pages 设置中选择 "GitHub Actions"
3. 推送到 `jekyll` 分支或手动触发工作流

**如果使用传统方式：**

1. 在 GitHub Pages 设置中选择分支和文件夹
2. GitHub 会自动使用 Jekyll 构建（如果仓库包含 `_config.yml` 和 `Gemfile`）

### 方法二：手动部署 Jekyll 版本

#### 步骤 1：本地构建 Jekyll 站点

```bash
# 安装依赖
bundle install

# 构建站点（输出到 _site 目录）
bundle exec jekyll build
```

#### 步骤 2：部署到 gh-pages 分支

```bash
# 方法 A：使用 gh-pages gem
gem install gh-pages
gh-pages -d _site

# 方法 B：手动推送到 gh-pages 分支
git checkout --orphan gh-pages
git rm -rf .
cp -r _site/* .
git add .
git commit -m "Deploy Jekyll site"
git push origin gh-pages --force
```

#### 步骤 3：在 GitHub Pages 设置中选择 gh-pages 分支

1. 进入 Settings → Pages
2. Source 选择 `gh-pages` 分支
3. 文件夹选择 `/ (root)`

## 🔍 验证切换

1. **检查构建输出**：
   - 查看 GitHub Actions 日志（如果使用）
   - 确认 `_site` 目录包含生成的 HTML 文件

2. **检查网站**：
   - 访问 `https://genomics-in-healthcare.github.io/`
   - 确认页面正常加载
   - 检查导航、链接是否正常

3. **检查功能**：
   - 成员页面是否正常显示
   - 新闻/博客文章是否正常显示
   - 静态资源（图片、CSS）是否正常加载

## 📝 注意事项

### 1. 保留两个版本

如果你想同时保留两个版本：

- **React 版本**：在 `main` 分支，使用 GitHub Actions 部署
- **Jekyll 版本**：在 `jekyll` 分支，使用传统 Jekyll 构建

### 2. 数据同步

如果切换后需要更新内容：

- **Jekyll 版本**：直接编辑 `_posts/*.md`, `_members/*.md` 等文件
- **React 版本**：需要运行 `npm run convert-members` 等转换脚本

### 3. 静态资源

两个版本共享 `static/` 目录中的静态资源，无需额外处理。

### 4. 回退到 React

如果需要切回 React 版本：

1. 在 GitHub Pages 设置中选择 "GitHub Actions"
2. 确保 `.github/workflows/deploy.yml` 已启用
3. 推送到 `main` 分支

## 🛠️ 故障排查

### 问题 1：Jekyll 构建失败

**解决方案**：
- 检查 `_config.yml` 语法是否正确
- 确认所有必需的插件在 `Gemfile` 中
- 查看 GitHub Actions 日志获取详细错误信息

### 问题 2：页面显示 404

**解决方案**：
- 确认 GitHub Pages 设置正确
- 检查 `baseurl` 在 `_config.yml` 中的配置
- 对于 `username.github.io` 仓库，`baseurl` 应为空字符串 `""`

### 问题 3：样式或资源加载失败

**解决方案**：
- 检查静态资源路径是否正确
- 确认 `_config.yml` 中的 `url` 配置正确
- 检查资源文件是否在正确的目录中

## 📚 相关文档

- [Jekyll 官方文档](https://jekyllrb.com/docs/)
- [GitHub Pages 文档](https://docs.github.com/en/pages)
- [项目 README](./README.md)
- [React 版本文档](./README-REACT.md)

## 🔄 快速切换命令

```bash
# 切换到 Jekyll 分支并部署
git checkout jekyll
git push origin jekyll

# 然后在 GitHub 设置中：
# Settings → Pages → Source: jekyll branch → / (root)
```

---

**提示**：建议在切换前先在本地测试 Jekyll 版本，确保一切正常：

```bash
bundle install
bundle exec jekyll serve
# 访问 http://localhost:4000 查看效果
```



