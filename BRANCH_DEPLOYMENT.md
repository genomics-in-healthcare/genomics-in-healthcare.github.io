# 使用分支部署方式 (Deploy from a branch)

本指南说明如何使用 GitHub Pages 的 "Deploy from a branch" 方式部署 Jekyll 网站。

## 📋 当前配置

您的网站已配置为使用 **"Deploy from a branch"** 方式。GitHub Pages 会自动检测 Jekyll 站点并构建。

## ⚙️ GitHub Pages 设置

### 步骤 1：进入 Pages 设置

1. 进入您的 GitHub 仓库
2. 点击 **Settings** 标签
3. 在左侧菜单中找到 **Pages** 选项

### 步骤 2：配置部署源

在 **Source** 部分：

1. **Branch**: 选择 `main`（或您想要部署的分支）
2. **Folder**: 选择 `/ (root)`

**重要**: 选择 `/ (root)` 让 GitHub 自动检测并构建 Jekyll 站点。如果选择 `/_site`，您需要先本地构建。

### 步骤 3：保存设置

点击 **Save** 保存设置。

## 🔍 工作原理

当您使用 "Deploy from a branch" 方式时：

1. GitHub Pages 会自动检测仓库中的 `_config.yml` 和 `Gemfile`
2. 使用 GitHub 的 Jekyll 构建服务自动构建网站
3. 构建结果会自动部署到 `https://genomics-in-healthcare.github.io/`

## ✅ 配置检查清单

确保以下配置正确：

- [x] `_config.yml` 存在且配置正确
- [x] `baseurl: ""` 已设置（对于 `username.github.io` 类型仓库）
- [x] `url: "https://genomics-in-healthcare.github.io/"` 已设置
- [x] `Gemfile` 存在且包含 `github-pages` gem
- [x] GitHub Pages 设置中选择 `/ (root)` 文件夹

## 📝 更新网站内容

### 添加或更新内容

1. 编辑相应的文件（如 `_posts/*.md`, `_members/*.md` 等）
2. 提交更改：
   ```bash
   git add .
   git commit -m "更新网站内容"
   git push origin main
   ```
3. GitHub Pages 会自动检测更改并重新构建（通常需要几分钟）

### 查看构建状态

1. 进入仓库的 **Actions** 标签
2. 查看 "pages build and deployment" 工作流
3. 如果构建失败，点击查看详细错误信息

## 🛠️ 故障排查

### 问题 1：构建失败

**可能原因**:
- `_config.yml` 语法错误
- `Gemfile` 中缺少必需的 gem
- 使用了 GitHub Pages 不支持的插件

**解决方案**:
1. 检查 GitHub Pages 构建日志（Settings → Pages → 查看构建历史）
2. 确保 `Gemfile` 只包含 GitHub Pages 支持的插件
3. 检查 `_config.yml` 的 YAML 语法

### 问题 2：页面显示 404

**可能原因**:
- `baseurl` 配置不正确
- 文件路径错误

**解决方案**:
- 确保 `baseurl: ""`（空字符串）在 `_config.yml` 中
- 检查文件路径和 permalink 配置

### 问题 3：样式或资源加载失败

**可能原因**:
- 静态资源路径不正确
- `url` 配置错误

**解决方案**:
- 在模板中使用 `{{ site.baseurl }}/static/...` 引用静态资源
- 确保 `url` 在 `_config.yml` 中正确配置

### 问题 4：更改后网站未更新

**解决方案**:
1. 等待几分钟（GitHub Pages 构建需要时间）
2. 强制刷新浏览器（Ctrl+F5 或 Cmd+Shift+R）
3. 检查构建状态（Settings → Pages）

## 🔄 切换到 GitHub Actions 方式

如果您想切换到 GitHub Actions 方式：

1. 在 GitHub Pages 设置中，将 **Source** 改为 **GitHub Actions**
2. 取消注释 `.github/workflows/deploy-jekyll.yml` 中的 `push` 触发器
3. 推送到 `main` 分支触发构建

## 📚 相关资源

- [GitHub Pages 文档](https://docs.github.com/en/pages)
- [Jekyll 官方文档](https://jekyllrb.com/docs/)
- [GitHub Pages 支持的插件](https://pages.github.com/versions/)

## 💡 提示

- **构建时间**: GitHub Pages 构建通常需要 1-5 分钟
- **缓存**: 如果更改未显示，尝试清除浏览器缓存
- **本地测试**: 在推送前，建议本地测试（`bundle exec jekyll serve`）
- **构建限制**: GitHub Pages 有构建时间限制，如果构建时间过长可能会失败

---

**当前部署方式**: Deploy from a branch (`/ (root)`)  
**构建服务**: GitHub Pages Jekyll 构建服务（自动）
















